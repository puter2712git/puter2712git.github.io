---
title: "Sneaking 플레이어 이동"
excerpt: "Sneaking 플레이어 이동"

categories:
  - Sneaking
tags:
  - Unity Project
  - Sneaking

toc: true
toc_sticky: true

date: 2022-12-28
last_modified_at: 2022-12-28
---

## 기본 구성

![image](https://user-images.githubusercontent.com/85764911/209820625-c323d6e9-6990-4c93-aea5-74830095462a.png)

기본 오브젝트 Plane과 외부 에셋 모델을 가져와서 씬을 가볍게 구성한다.

<br>

## 플레이어 WASD Input 감지

### PlayerInput.cs
```cs
using UnityEngine;

public class PlayerInput : MonoBehaviour
{
	/* Detect WASD input */
	public float horizontalMove { private set; get; }
	public float verticalMove { private set; get; }

	private void Update()
	{
		/* Get inputs of move */
		horizontalMove = Input.GetAxisRaw("Horizontal");
		verticalMove = Input.GetAxisRaw("Vertical");
	}
}
```

매 프레임마다 Input을 WASD로 들어오는 Input을 감지한다.

<br>

## 플레이어 WASD 이동

### PlayerMove.cs
```cs
using UnityEngine;

public class PlayerMove : MonoBehaviour
{
	private PlayerInput _playerInput;
	private Rigidbody _rigidbody;

	[SerializeField]
	private float _moveSpeed;
	private float _horizontalMove;
	private float _verticalMove;

	private void Awake()
	{
		_playerInput = GetComponent<PlayerInput>();
		_rigidbody = GetComponent<Rigidbody>();
	}

	private void FixedUpdate()
	{
		Move();
	}

	private void Move()
	{
		_horizontalMove = _playerInput.horizontalMove;
		_verticalMove = _playerInput.verticalMove;

		_rigidbody.velocity = new Vector3(_horizontalMove, 0f, _verticalMove).normalized * _moveSpeed;
	}
}
```

`Move()` 메서드에서 오브젝트의 속도 제어 <br>
대각선으로 이동할 때도 속도가 같아야 하기 때문에 normalize한 뒤 속력을 곱해준다.

